packages: []

allowBuilds:
  # vitest's bundler, and the worker runtime for wrangler.
  esbuild: true
  workerd: true
  sharp: true
  # Compiles the native SQLite binding for the local service. Skipping it
  # leaves a package that imports cleanly and throws at first query.
  better-sqlite3: true
